# Deploy Estático (Hostinger)

1. Ejecuta:

```bash
npm run build
```

2. Next.js generará la carpeta `out/` con `index.html` y assets estáticos.

3. Sube todo el contenido de `out/` al directorio `public_html` de Hostinger.
